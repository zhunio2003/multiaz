spring:
  application:
    name: prediction-orchestrator
  config:
    import: optional:configserver:http://config-service-multiaz:8888
  profiles:
    active: dev

server:
  port: 8084

eureka:
  client: 
    serviceUrl:
      defaultZone: http://service-discovery-multiaz:8761/eureka/
    