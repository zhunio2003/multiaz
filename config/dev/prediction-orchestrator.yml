spring:
  datasource:
    url: jdbc:postgresql://postgresql:5432/prediction_orchestrator  
    username: ${POSTGRESQL_USER}
    password: ${POSTGRESQL_PASS}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration    